# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=quan_ly_nvqs

# Google Gemini (https://aistudio.google.com/apikey)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# Auth
JWT_SECRET=REPLACE_WITH_RANDOM_SECRET_GENERATED_LOCALLY

# Generate JWT_SECRET: openssl rand -hex 48
ALLOW_DEMO_AUTH=false
# Enable only behind a reverse proxy that overwrites X-Forwarded-For
TRUST_PROXY=false
